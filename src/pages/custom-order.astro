---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Custom Order - Crocheted Animals"
  description="Request a custom handcrafted crocheted animal. Choose your own colors, sizes, and designs."
>
  <section class="py-16 md:py-24">
    <div class="container mx-auto px-4">
      <div class="max-w-2xl mx-auto">
        <!-- Header -->
        <div class="text-center mb-12">
          <h1 class="font-serif text-4xl md:text-5xl font-bold text-stone-800 mb-4">
            Custom Order Request
          </h1>
          <p class="text-lg text-stone-600">
            Have a special vision? Let's create something unique together! Describe your dream crocheted companion below.
          </p>
        </div>

        <!-- Custom Order Form -->
        <form
          name="custom-order"
          method="POST"
          data-netlify="true"
          netlify-honeypot="bot-field"
          class="bg-white rounded-lg shadow-lg p-8"
        >
          <!-- Netlify Form Setup -->
          <input type="hidden" name="form-name" value="custom-order" />
          <p class="hidden">
            <label>
              Don't fill this out if you're human: <input name="bot-field" />
            </label>
          </p>

          <!-- Animal Type -->
          <div class="mb-6">
            <label for="animal-type" class="block text-sm font-semibold text-stone-700 mb-2">
              What type of animal? <span class="text-rose-600">*</span>
            </label>
            <input
              type="text"
              id="animal-type"
              name="animal-type"
              required
              class="w-full px-4 py-3 border border-stone-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-500 focus:border-transparent"
              placeholder="e.g., Unicorn, Dragon, Cat, etc."
            />
          </div>

          <!-- Description -->
          <div class="mb-6">
            <label for="description" class="block text-sm font-semibold text-stone-700 mb-2">
              Describe your vision <span class="text-rose-600">*</span>
            </label>
            <textarea
              id="description"
              name="description"
              required
              rows="5"
              class="w-full px-4 py-3 border border-stone-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-500 focus:border-transparent"
              placeholder="Tell us about your ideal crocheted companion. What features are important? Any specific characteristics?"
            ></textarea>
          </div>

          <!-- Size Preference -->
          <div class="mb-6">
            <label for="size" class="block text-sm font-semibold text-stone-700 mb-2">
              Preferred Size
            </label>
            <select
              id="size"
              name="size"
              class="w-full px-4 py-3 border border-stone-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-500 focus:border-transparent"
            >
              <option value="">No preference</option>
              <option value="small">Small (6-8 inches)</option>
              <option value="medium">Medium (10-12 inches)</option>
              <option value="large">Large (14-18 inches)</option>
              <option value="custom">Custom size (specify in notes)</option>
            </select>
          </div>

          <!-- Color Preferences -->
          <div class="mb-6">
            <label for="colors" class="block text-sm font-semibold text-stone-700 mb-2">
              Color Preferences
            </label>
            <input
              type="text"
              id="colors"
              name="colors"
              class="w-full px-4 py-3 border border-stone-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-500 focus:border-transparent"
              placeholder="e.g., Pink and purple, Natural earth tones, Rainbow colors"
            />
          </div>

          <!-- Budget -->
          <div class="mb-6">
            <label for="budget" class="block text-sm font-semibold text-stone-700 mb-2">
              Budget Range
            </label>
            <select
              id="budget"
              name="budget"
              class="w-full px-4 py-3 border border-stone-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-500 focus:border-transparent"
            >
              <option value="">Flexible</option>
              <option value="30-50">$30-$50</option>
              <option value="50-75">$50-$75</option>
              <option value="75-100">$75-$100</option>
              <option value="100+">$100+</option>
            </select>
          </div>

          <!-- Customer Name -->
          <div class="mb-6">
            <label for="name" class="block text-sm font-semibold text-stone-700 mb-2">
              Your Name <span class="text-rose-600">*</span>
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full px-4 py-3 border border-stone-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-500 focus:border-transparent"
              placeholder="John Doe"
            />
          </div>

          <!-- Email -->
          <div class="mb-6">
            <label for="email" class="block text-sm font-semibold text-stone-700 mb-2">
              Email Address <span class="text-rose-600">*</span>
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-3 border border-stone-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-500 focus:border-transparent"
              placeholder="john@example.com"
            />
          </div>

          <!-- Additional Notes -->
          <div class="mb-6">
            <label for="notes" class="block text-sm font-semibold text-stone-700 mb-2">
              Additional Notes or Questions
            </label>
            <textarea
              id="notes"
              name="notes"
              rows="3"
              class="w-full px-4 py-3 border border-stone-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-rose-500 focus:border-transparent"
              placeholder="Any other details or questions for us?"
            ></textarea>
          </div>

          <!-- Submit Button -->
          <button
            type="submit"
            class="w-full bg-rose-600 text-white px-8 py-4 rounded-lg font-semibold hover:bg-rose-700 transition-colors shadow-lg hover:shadow-xl"
          >
            Submit Custom Request
          </button>

          <!-- Info Box -->
          <div class="mt-6 p-4 bg-amber-50 border border-amber-200 rounded-lg">
            <p class="text-sm text-amber-800">
              <strong>Custom Order Process:</strong> We'll review your request and respond within 1-2
              business days with a design proposal, timeline, and pricing. Most custom orders are
              completed within 2-3 weeks.
            </p>
          </div>
        </form>

        <!-- Examples Section -->
        <div class="mt-16">
          <h2 class="font-serif text-3xl font-bold text-stone-800 mb-6 text-center">
            Custom Order Examples
          </h2>
          <div class="grid md:grid-cols-2 gap-6">
            <div class="bg-stone-50 p-6 rounded-lg">
              <h3 class="font-bold text-lg mb-2 text-stone-800">Color Variations</h3>
              <p class="text-stone-600 text-sm">
                Choose any of our existing designs in your favorite colors. Perfect for matching
                nursery themes or personal preferences.
              </p>
            </div>
            <div class="bg-stone-50 p-6 rounded-lg">
              <h3 class="font-bold text-lg mb-2 text-stone-800">Size Adjustments</h3>
              <p class="text-stone-600 text-sm">
                Need something smaller or larger? We can adjust the size of most designs to fit
                your needs.
              </p>
            </div>
            <div class="bg-stone-50 p-6 rounded-lg">
              <h3 class="font-bold text-lg mb-2 text-stone-800">Unique Characters</h3>
              <p class="text-stone-600 text-sm">
                Have a favorite animal or character not in our gallery? We love creating new
                designs and bringing your imagination to life!
              </p>
            </div>
            <div class="bg-stone-50 p-6 rounded-lg">
              <h3 class="font-bold text-lg mb-2 text-stone-800">Special Accessories</h3>
              <p class="text-stone-600 text-sm">
                Add personalized touches like bows, scarves, hats, or even embroidered names to
                make your companion extra special.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
